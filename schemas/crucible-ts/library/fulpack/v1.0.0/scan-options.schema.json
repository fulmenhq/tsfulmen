{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.fulmenhq.dev/crucible/library/fulpack/v1.0.0/scan-options",
  "title": "Scan Options",
  "description": "Options for archive scanning operation (for Pathfinder integration)",
  "type": "object",
  "properties": {
    "include_metadata": {
      "type": "boolean",
      "default": true,
      "description": "Include metadata (size, checksum, modified timestamp) in results"
    },
    "entry_types": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "file",
          "directory",
          "symlink"
        ]
      },
      "description": "Filter entries by type (from entry-types taxonomy)"
    },
    "max_depth": {
      "type": [
        "integer",
        "null"
      ],
      "minimum": 0,
      "default": null,
      "description": "Maximum depth for directory traversal (null = unlimited)"
    },
    "max_entries": {
      "type": "integer",
      "minimum": 0,
      "default": 100000,
      "description": "Safety limit for maximum entries to return"
    }
  },
  "additionalProperties": false
}
