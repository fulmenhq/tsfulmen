{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.fulmenhq.dev/gofulmen/pathfinder/finder-config-v1.0.0.json",
  "title": "FinderConfig",
  "description": "Configuration for pathfinder Finder instances",
  "type": "object",
  "properties": {
    "maxWorkers": {
      "type": "integer",
      "description": "Maximum number of worker goroutines",
      "minimum": 1,
      "default": 4
    },
    "cacheEnabled": {
      "type": "boolean",
      "description": "Whether caching is enabled",
      "default": false
    },
    "cacheTTL": {
      "type": "integer",
      "description": "Cache TTL in seconds",
      "minimum": 0,
      "default": 300
    },
    "constraint": {
      "$ref": "#/$defs/pathConstraint",
      "description": "Path constraint configuration"
    },
    "loaderType": {
      "type": "string",
      "description": "Default loader type",
      "enum": ["local", "remote", "cloud"],
      "default": "local"
    }
  },
  "$defs": {
    "pathConstraint": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": ["repository", "workspace", "cloud"],
          "description": "Type of path constraint"
        },
        "enforcementLevel": {
          "type": "string",
          "enum": ["strict", "warn", "permissive"],
          "description": "How strictly constraints are enforced"
        },
        "root": {
          "type": "string",
          "description": "Root path for constraint"
        }
      }
    }
  }
}
